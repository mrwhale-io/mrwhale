import { Activities } from "./activities";

/**
 * Options for the activity handler.
 */
export interface ActivityHandlerOptions {
  /**
   * The name of the activity handler.
   */
  name: Activities;
}
